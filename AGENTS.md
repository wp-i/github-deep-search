# 开发与测试约束

修改本项目之前，先阅读 `docs/PRODUCT_CONTRACT.md`、
`docs/ARCHITECTURE.md` 和 `docs/TESTING.md`。产品契约优先于现有实现；旧代码、
旧测试和旧文档不能作为偏离当前契约的理由。

1. 不允许通过临时补丁让问题消失或让测试通过。调整前先定位产生错误的原逻辑，
   优先修正唯一的行为所有者，不得在下游增加补偿分支。
2. 对所有已发现异常都遵守最小范围改动原则。只修改已确认的责任边界，严禁从单个
   问题无限扩展到无关模块。
3. 执行测试前，先确认测试标准符合最新产品契约和用户预期。旧需求对应的断言不得
   阻碍正确实现，也不得被改写成另一种样例特判。
4. 功能不得过度耦合到同一个方法、模块或页面。六个阶段、运行管理、Provider 和
   Web 展示分别拥有自己的职责。
5. 产品语义只能来自当前用户输入、当前 LLM 解析和当前 GitHub 仓库材料。禁止向
   运行时加入静态业务词表、翻译表、样例关键词、仓库名单或已知输入分支。
6. 一个行为只能有一个实现所有者。新逻辑取代旧逻辑时，同步删除被取代的分支、
   helper、测试和文档，不保留兼容补丁。
7. 先修改契约，再修改实现和测试。同一行为的文档、数据模型、实现与测试必须描述
   同一版本。
8. GitHub 或 LLM 的鉴权、限流、网络和输出结构错误必须显式失败或警告。禁止匿名
   GitHub 降级、伪造结果、静默跳过关键失败或用字面关键词搜索替代 LLM 解析。
9. 先运行确定性测试，再进行最小必要的真实 Provider 验证。真实验证首次失败后停止
   扩大调用，先定位所属阶段；普通改动不强制增加独立评审 LLM。
10. Token、API Key、Authorization Header 和其他凭据不得进入源码、日志、报告、
    测试夹具或提交产物。错误信息和调试数据必须脱敏。
